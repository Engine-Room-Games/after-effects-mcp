// boot.jsx — entry stub. The panel's main.js calls $.evalFile on bundle.jsx,
// so this file is mostly a placeholder. Keeping it for CSXS manifest compliance.
$.global._aeMcpBootedAt = (new Date()).getTime();
